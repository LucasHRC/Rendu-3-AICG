{
  "name": "local-llm-literature-reviewer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tts:server": "cd tts-server && npm install && npm start",
    "dev:all": "concurrently \"npm run dev\" \"npm run tts:server\" --names \"VITE,TTS\" --prefix-colors \"blue,green\""
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
